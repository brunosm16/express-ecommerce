class ParentTable {
	constructor(ParentModel, parentId, parentName) {
		this.ParentModel = ParentModel;
		this.parentId = parentId;
		this.parentName = parentName;
	}
}

module.exports = ParentTable;
