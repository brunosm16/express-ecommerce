const usersMessages = require('./users');
const addressesMessages = require('./addresses');
const categories = require('./categories');

module.exports = { usersMessages, addressesMessages, categories };
