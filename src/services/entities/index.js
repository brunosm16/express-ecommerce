const validateEntity = require('./validate-entity');
const persistEntity = require('./persist-entity');
const removeEntity = require('./remove-entity');

module.exports = {
	validateEntity,
	persistEntity,
	removeEntity,
};
